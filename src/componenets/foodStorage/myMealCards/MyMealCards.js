//This will load the data from the mealPackets Database

export const MyMealCards = () => {
    return(
        <>
            <p>This too should also be filled with lots of data</p>
        </>
    )
}